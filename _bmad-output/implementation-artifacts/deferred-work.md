# Deferred Work

<!-- Labels: [resolved] = done, [pre-epic-3] = address before/during Epic 3, [pre-epic-4] = owned by Epic 4, [optional] = nice-to-have, no story dependency -->

## Deferred from: code review of 1-1-initialize-monorepo-scaffold-with-toolchain (2026-04-30)

- **[optional]** `bun-types` is declared in `apps/web/devDependencies` and listed in `apps/web/tsconfig.json` `"types"`, even though no SPA source consumes Bun-only globals. The architectural boundary says only `apps/api/src/env.ts` should consume `Bun.env`. Attempted to drop it; reverted because `tsgo -b --noEmit` resolves `bun-types` for `apps/api` as `primary: false` and the global `Bun` namespace stops loading when only `apps/api` declares the type. Revisit when (a) tsgo's project-references type resolution improves, (b) we switch from `bun-types` to `@types/bun` (the modern Microsoft-maintained types), or (c) we drop `moduleResolution: bundler` for `apps/api`. In the meantime, an oxlint rule could forbid `Bun\.` and `import.meta.dir` in `apps/web/src/**` to catch any leak before it ships.
- **[optional]** `SPA_DIST` is resolved at module load relative to `import.meta.dir` in `apps/api/src/index.ts:6`. This breaks any future `bun build --compile` workflow that produces a single binary, because the compiled binary's `import.meta.dir` no longer points at the dist tree. Revisit when a binary-bundling story arrives; allow override via env var.
- **[optional]** `process.stdout.write` of the structured "listening" line in `apps/api/src/index.ts:46` is not awaited or explicitly flushed. There is a theoretical race where `app.listen` fails or the process exits before the line reaches a downstream reader (e.g. `lhci`'s `startServerReadyPattern: "listening"`). lhci is currently working per the Change Log, so defer until we see a flake.
- **[optional]** `check:release` runs `lhci autorun` twice (mobile, then desktop), both binding port 3000 via the same `startServerCommand`. If the first run leaves the API server alive, the second `EADDRINUSE`s. lhci historically cleans up between runs, so defer until a flake appears; if it does, switch the desktop run to a separate port or add a wait gate.

## Deferred from: code review of 1-2-backend-plumbing-logger-error-envelope-request-middleware-health-endpoint (2026-04-30)

- **[optional]** `apps/api/src/log.ts:23` — `JSON.stringify({ level, msg, ts, ...extra })` will throw on circular references and on BigInt-typed field values. Today's callers pass narrow types, so risk is low, but a future caller logging a request body or domain entity could crash the request path. Add a JSON.stringify replacer (WeakSet seen-tracker + BigInt → string) or wrap `emit` in try/catch with a fallback line.
- **[optional]** `apps/api/src/middleware/requestLogger.test.ts` — no concurrent-request test asserts that two overlapping `app.handle()` calls produce distinct, non-interleaved `requestId` values. The `WeakMap<Request, string>` design is correct for concurrency, but a regression introducing module-level mutable state would pass all current tests. Add a test that fires two `app.handle()` promises in parallel and asserts each pair of entry/exit logs shares an id distinct from the other pair.
- **[optional]** `apps/api/src/onError.test.ts` — no test asserts `Content-Type: application/json` on error envelope responses. `routes/health.test.ts` checks it for the happy path; error responses do not. If a future Elysia upgrade emits envelopes as `text/plain`, strict client parsers break silently.
- **[resolved]** `apps/api/src/errors/AppError.ts` — class does not accept the standard `ErrorOptions.cause` option. Wrapping an upstream DB/network error (Stories 1.3+) loses the original stack and message. Add an optional `cause` field passed through to `super(message, { cause })`. *(Fixed in retro-prep commit 4328fe1)*
- **[optional]** `apps/api/src/middleware/requestLogger.ts:33`, `apps/api/src/onError.ts:15` — `requestLogger` and `registerOnError` return `AnyElysia`, which erases the chain's `derive`/`decorate` typing for downstream `app.use(...)` consumers. The Debug Log records this as an intentional tradeoff to avoid dragging the deeply-generic plugin type through the codebase. Track for future re-typing once Elysia's plugin type ergonomics improve.

## Deferred from: code review of 1-3-backend-persistence-tasks-table-forward-only-migrations-repository-skeleton (2026-04-30)

- **[optional]** `apps/api/src/storage/db.ts:14-19` — `db()` lazy singleton has no `close` / shutdown hook, no test reset, and no swap. WAL files may not checkpoint cleanly on process restart. Address when a graceful-shutdown story arrives (likely alongside Story 1.11 deployment).
- **[optional]** `apps/api/src/storage/migrations/runner.ts:23-55` — Concurrent migration runners against the same SQLite file have no advisory lock or `BEGIN IMMEDIATE` guard. Two booting processes can both observe `applied=∅`, both attempt `CREATE TABLE tasks`, the second fails inside its wrapping transaction. Today's deployment is single-process per container so this is theoretical; revisit if the deployment model adds workers or rolling restarts that overlap.
- **[optional]** `apps/api/src/index.ts:13-25` — Migration failure at boot is sticky (no retry). If the SQLite file is briefly unavailable on a network mount, /health returns 503 forever until the operator restarts the container. Consider a bounded retry-with-backoff in the boot block, or a separate "retry migrations" admin endpoint.
- **[optional]** `apps/api/src/storage/tasks.ts:60-72` — `INSERT OR IGNORE` swallows all constraint violations (PK, CHECK on text length, future CHECK on completed). Out-of-bounds text from an unvalidated path becomes a misleading "invariant: task missing after INSERT OR IGNORE" throw. Story 1.4's router-level validation is the primary guard; consider adding defense-in-depth at the repo boundary or translating SQLITE_CONSTRAINT_CHECK to a typed validation error.
- **[optional]** `apps/api/src/storage/migrations/001_create_tasks.up.sql:2` — Schema does not constrain `id` format. The repo's `ORDER BY id DESC` newest-first contract assumes UUIDv7 monotonicity; a non-UUIDv7 id injected via a future path would break ordering invisibly. Add a CHECK constraint or repository-level validation when Story 1.4 ships id-validation logic.
- **[optional]** `apps/api/src/storage/tasks.ts:21-29` — `list()` returns every row (no LIMIT, no pagination). Acceptable for MVP but unbounded as the table grows. Revisit when product scope adds filtering / archival.
- **[optional]** `apps/api/src/storage/migrations/runner.ts:32-34` — Lexical sort mis-orders 4-digit and 3-digit prefixes (`1000_y.up.sql` < `999_x.up.sql`). Spec acknowledges this; switch to numeric sort by parsed version (or fixed-width prefix) before crossing version 999.
- **[optional]** `apps/api/src/storage/db.ts:14-19`, `apps/api/src/storage/tasks.ts:91-103` — Lazy singletons retry the open on every call after a partial PRAGMA failure. No memoization of failure → potential reopen storm under transient errors. Consider a one-shot failure cache.
- **[pre-epic-3]** `apps/api/src/storage/tasks.ts:60-73` — The "INSERT OR IGNORE no-op + row no longer exists" path is theoretically reachable today (manual SQL outside the repo) and becomes a real concurrency path once Story 3.1 implements `delete`. Decide between re-INSERT-without-IGNORE or documenting the no-delete invariant explicitly when Story 3.1 lands. *Story 3.1 must resolve this.*
- **[optional]** `apps/api/src/storage/migrations/runner.ts:44-50` — Migration files containing explicit `BEGIN; ... COMMIT;` collide with the wrapping `db.transaction(...)` and fail with `cannot start a transaction within a transaction`. Document the convention "do not include BEGIN/COMMIT in migration files" in a comment near the runner, or strip them defensively.
- **[optional]** `apps/api/src/index.ts:21-25` — `new Error(String(err))` coerces non-Error throws (e.g., a thrown SQLite-error-like object whose `instanceof Error` returns false in some bun:sqlite versions) and discards the original `code`/cause. Log the raw `err` alongside the synthesized Error so SQLite-specific error codes survive into operator logs.
- **[optional]** `apps/api/src/storage/db.test.ts` — Default state of the readiness module (`getDbStatus()` returns `{ ready: false, error: undefined }` at module load) is not asserted in tests. Bun's test runner shares module state across test files in a single run, so once `index.test.ts`'s import-time `setDbReady()` fires, `_ready` is contaminated for all subsequent test files. The default is verifiable only via process isolation (e.g., a separate `bun test` invocation against this file alone) or by reading the source. Revisit when test isolation tooling improves, or add a guard in `index.ts` so the boot block does not run during `bun test`.

## Deferred from: code review of 1-5-frontend-foundation-solid-app-design-tokens-eden-client-errorboundary-liveregion (2026-04-30)

- **[optional]** `apps/web/src/data/uuid.ts:13-22` — UUIDv7 sequence overflow path (`lastTimestampMs += 1; sequence = 0`) has no upper bound and no recovery when wall-clock catches up. Sustained >4096 IDs/ms or a backward NTP step drifts the embedded timestamp arbitrarily into the future, breaking lexicographic-time ordering against any externally-generated UUIDv7 (server, other clients). Acceptable while capture is keyboard-driven and single-client; revisit if Story 1.8+ adds bulk import or if the backend ever issues UUIDv7s.
- **[resolved]** `apps/web/src/components/LiveRegion.tsx:3-8` — `announce()` is built around a module-scope `createSignal`. Three latent issues: (1) synchronous collapse, (2) pre-mount silence, (3) HMR multi-mount. *(Queue + drain loop + refcount mount tracking added in retro-prep commit 4328fe1; generation token added to guard stale closures)*
- **[optional]** `apps/web/uno.config.ts:21-32` — The `bg-token-*` / `border-token-*` / `text-token-*` rule regexes match any string suffix and look up `themeVars[token]`. Typos like `bg-token-bgcanvas` produce CSS with `background: undefined` instead of failing loudly. Add a membership guard (`token in themeVars ? value : undefined`) once the codebase actually adopts these utilities and a typo would slip past review.
- **[optional]** `apps/web/src/styles/reset.css:88-95` — `prefers-reduced-motion: reduce` sets `animation-duration` and `transition-duration` to `0ms !important` rather than disabling animations outright. WebKit's recommended pattern is `animation: none !important; transition: none !important;` (or `0.01ms` with `animation-iteration-count: 1`) because zero-duration animations still fire `animationiteration`/`animationend` events and consume CPU on infinite loops. Defer until any story actually introduces motion.
- **[resolved]** `apps/web/src/App.tsx:6` — User-facing `<h1>` reads "bmad-todo-app", which is the meta/codebase identifier rather than a real product name. *(Fixed in retro-prep commit 4328fe1: `index.html` title updated to "Tasks"; no h1 element in current app)*
- **[optional]** `apps/web/src/components/LiveRegion.test.tsx`, `apps/web/src/components/ErrorBoundary.test.tsx` — Component tests `readFileSync` the source and assert on substrings rather than rendering. Revisit when a `happy-dom` global registrator (or equivalent) is added under a bun-test preload so the helpers and `@solidjs/testing-library` can drive real assertions.

## Deferred from: code review of 1-6-taskinput-component-with-auto-focus-submit-and-character-counter (2026-04-30)

- **[optional]** `apps/web/package.json:devDependencies` — Four devDeps were added beyond the two AC10 + the Anti-Patterns list authorize as the *only* additions: `bun-plugin-solid@1.0.0`, `babel-preset-solid@1.9.12`, `@babel/core@7.29.0`, `@babel/preset-typescript@7.28.5`. Solid does not ship a React-style automatic JSX runtime, so without bun-plugin-solid (and its babel peers) `@solidjs/testing-library` cannot render under Bun's test loader. Documented as a forced deviation in the Dev Agent Record. Revisit if (a) Solid publishes a Bun-compatible JSX runtime, (b) we adopt a different test runner for the SPA (Vitest under Vite already has the plugin chain), or (c) the babel deps' transitive footprint shows up as a real install-cost or audit-surface concern.
- **[pre-epic-4]** `apps/web/src/components/TaskInput.test.tsx:59, 90` — "Retains focus after Enter / Escape" assertions read `document.activeElement === input` immediately after `fireEvent.keyDown`. happy-dom does not run the browser's full focus management around synthetic keyboard events, so the assertion is trivially true — the test cannot fail today. AC#3 / AC#4 require real focus retention behavior; verification properly belongs in a Playwright spec. Revisit alongside Story 4.5's keyboard-only Playwright audit.

## Deferred from: code review of 1-7-task-list-display-tasklist-taskrow-active-state-emptystate-loadingstate (2026-04-30)

- **[optional]** `apps/web/src/data/api.ts` — `(api.api as unknown as { tasks: { get: () => Promise<TasksGetResponse> } }).tasks.get()` discards Eden's actual end-to-end response shape. Documented as deviation #4. The cast is the only API call seam in the SPA, so any backend response shape drift compiles clean and breaks at runtime. Revisit when a clean indirection-wrapper / Eden type-pin pattern is found that doesn't require `as unknown as`.
- **[resolved]** `apps/web/src/data/api.ts` — `tasksApi` is a mutable module-level object literal so tests can do `tasksApi.list = mock(...)`. *(Fixed in retro-prep commit 4328fe1: `_tasksApiSeams` extracted as dedicated DI seam; `tasksApi` methods no longer mutated in tests)*
- **[optional]** `apps/web/src/components/TaskList.test.tsx` — 200 ms loading-flash gate test waits real-time `LOADING_DELAY_MS + 50` ms (≈ 250 ms). Adds ~250 ms per CI run and risks flake under heavy CI load. Revisit when Bun's `setSystemTime` reliably advances `setTimeout`-scheduled callbacks.
- **[optional]** `apps/web/src/data/api.ts` — Error envelope path lookup at `(error.value as { error?: { message?: string } }).error?.message` only matches one envelope shape. Network failures, plain-text 5xx bodies, and CORS-opaque responses all fall through to the generic `HTTP {status}` fallback, masking real causes.
- **[optional]** `apps/web/src/components/TaskList.test.tsx` — AC #14 responsive coverage at three tiers reduced to a single populated render with structural class-presence assertions. Visual regression at three tiers is owned by a future Playwright visual-regression spec.
- **[optional]** `apps/web/src/components/TaskList.tsx` — `createEffect` reads `query.isPending` and registers `onCleanup(() => clearTimeout(...))` inside the `if` arm. Today only one accessor is tracked, so timer stacking is impossible. Revisit if future changes add another tracked accessor inside this effect.
- **[optional]** `apps/web/src/components/TaskList.tsx` — Resolution exactly at 200 ms can produce a one-frame skeleton flash. Edge case with no current visible defect.
- **[optional]** `apps/web/src/components/TaskRow.test.tsx` — XSS regression scan only iterates `task.text`. Expand when a future stringly-rendered task field is added.

## Deferred from: code review of 1-8-optimistic-task-creation-happy-path (2026-05-01)

- **[pre-epic-3]** `e2e/capture.spec.ts` — Test isolation: only test 5.1 honors the spec's delta-based assertion (`preCount + 1`); tests 5.2 / 5.3 / 5.4 rely on `Date.now()`-suffixed unique text rather than capturing pre-counts, and no `beforeEach` clears the dev SQLite DB. Revisit alongside Story 3.1 (DELETE endpoint becomes available — enables a clean fixture-reset hook) or when Playwright tests start flaking due to accumulated rows.
- **[resolved]** `apps/web/src/data/queries.ts:35` — `previous = queryClient.getQueryData<Task[]>(tasksQueryKey) ?? []` retains the cache's live array reference, not a deep snapshot. *(Fixed in retro-prep commit 4328fe1: `useCreateTask` now does `const previous = [...(queryClient.getQueryData<Task[]>(tasksQueryKey) ?? [])]` — shallow copy)*

## Deferred from: code review of 1-9-sync-pending-indicator-and-retry-exhausted-state-for-capture (2026-05-01)

- **[resolved]** `apps/web/src/components/LiveRegion.tsx:12,38-43` — `mounted` is a single module-scoped boolean shared by all `<LiveRegion />` instances. *(Fixed in retro-prep commit 4328fe1: refcount mount tracking added; `onCleanup` decrements refcount instead of setting to false)*
- **[optional]** `apps/web/src/data/captureSyncStore.ts:22` — `__captureSyncMutators.clear(id)` writes `undefined` rather than deleting the key. The underlying record retains every key that has ever been mutated; store grows monotonically over long sessions. Address with `produce((draft) => { delete draft[id]; })` when a real growth signal appears.
- **[optional]** `apps/web/src/data/retryAfter.ts:9-11` — Fallback path uses `Date.parse(trimmed)` which accepts loose forms not conforming to RFC 7231 §7.1.3. The Story 1.4 backend emits well-formed `Retry-After`, so production is not affected.
- **[resolved]** `apps/web/src/data/queries.ts:108-117` — `useCreateTask` exposes a single `mutationObserver` shared across all rows. The per-row `retry()` callback is racy under concurrent mutations. *(Fixed in retro-prep commit 4328fe1: `observer.reset()` removed from retry closures; `observer.mutate(input)` is now the retry call, eliminating the reset-induced race)*
- **[optional]** `apps/web/src/components/LiveRegion.tsx:22-29` — `drain()` microtask/unmount interaction. Production has a single mount, so this is HMR-only.
- **[resolved]** `apps/web/src/components/LiveRegion.tsx:25-28` — `__resetLiveRegionForTests` does not invalidate in-flight microtasks. *(Fixed in retro-prep commit 4328fe1: generation token added to every microtask/setTimeout closure; stale closures check token before executing)*
- **[optional]** `apps/web/src/data/queries.ts:113-117` — The 300 ms pending timer fires during exponential-backoff sleep. Arguably correct UX; revisit if QA flags spinner during backoff as confusing.

## Deferred from: code review of 1-10-list-level-fetch-error-state-and-reconnection-recovery (2026-05-01)

- **[optional]** `apps/web/src/components/TaskList.tsx:24` — No loading affordance during the in-flight retry chain after a Retry click. Product decision deferred.
- **[pre-epic-4]** `apps/web/src/components/TaskList.tsx:48-54` — When Retry succeeds, focus moves to `<body>`. No focus restoration to TaskInput or to the list region. Story 4.3 owns Escape/i return-focus-to-TaskInput, which could extend to this transition.
- **[optional]** `apps/web/src/components/TaskList.tsx:24` — Optimistic rows in `queryClient` cache become invisible while `isError` is true. Acknowledged as acceptable trade-off.
- **[pre-epic-3]** `e2e/empty-error-states.spec.ts` — Tests run under `fullyParallel: true` against the shared dev SQLite DB. Assertions can race against rows created by `e2e/capture.spec.ts` running on a sibling worker. Revisit alongside Story 3.1 (DELETE endpoint enables a fixture-reset hook) or when Playwright failures actually flake.

## Deferred from: code review of 1-11-end-to-end-single-service-container-deployment (2026-05-01)

- **[resolved]** Image-size optimization. Runtime image was 447 MB with devDependencies in runtime stage. *(Fixed in retro-prep commit 4328fe1: `prod-deps` stage added with `bun install --production --frozen-lockfile`; runtime stage copies from `prod-deps`)*
- **[optional]** `HEALTHCHECK` directive in Dockerfile for Compose-level health probing. Defer to a hardening story.
- **[optional]** Non-root `USER bun` directive + read-only filesystem. Defer to a hardening story.
- **[optional]** `.dockerignore` extension to exclude `_bmad-output/`, `_bmad/`, `.claude/`, `node_modules/`, etc. Local-only cost is < 1 s today.
- **[optional]** Restart policy (`restart: unless-stopped` in `compose.yaml`). MVP intentionally omits.
- **[optional]** Image-registry push automation. Vision-phase.
- **[pre-epic-3]** Dev-DB cleanup hook for the e2e suite. `apps/api/tasks.db` accumulates across `bun run dev` invocations and contaminates Playwright assertions that count `getByRole("listitem")`. The deeper fix — a fixture-reset hook or per-test isolated DB — is enabled by Story 3.1's DELETE endpoint. *Story 3.1 should implement a `DELETE /api/tasks` (bulk) or a test-mode reset hook.*
- **[resolved]** `e2e/manage.spec.ts` and `e2e/keyboard.spec.ts` are placeholder stubs. *(manage.spec.ts has real toggle assertions from Story 2.2; keyboard.spec.ts has toggle tests from Story 2.3; Epic 4 navigation stub intentionally preserved)*
- **[optional]** `waitForListSettled` / `waitForInitialTasksLoad` deadlock when initial GET fails. Won't occur in current test suite.
- **[pre-epic-3]** `two captures land newest-first` sibling test not updated with `waitForListSettled` — same preCount race class exists. Revisit when DB row accumulation causes flake. *Story 3.1's fixture-reset hook resolves this.*
- **[optional]** Helper settle-timeout (5000ms) on same order as maximum retry backoff budget. Revisit if Playwright flakes on settle helpers.
- **[optional]** No `X-Content-Type-Options: nosniff` on static asset responses. Defense-in-depth gap.
- **[optional]** Source map files would be served without access control if sourcemaps enabled. Not a risk today.
- **[optional]** No `Cache-Control` headers on static assets. Add as a performance/operability follow-up.

## Deferred from: code review of 2-1-backend-patch-api-tasks-id-for-completion-toggle (2026-05-01)

- **[optional]** `apps/api/src/storage/tasks.ts` — TOCTOU gap: `UPDATE` and the subsequent `get(id)` SELECT run in separate SQLite statements. Single-threaded Bun runtime makes this theoretical. Spec explicitly allows the pattern.
- **[optional]** `apps/api/src/storage/tasks.ts` — `updatedAt` is unconditionally bumped even when the PATCH `completed` value matches the current row state (no-op idempotent call).
- **[optional]** `apps/api/src/routes/tasks.ts` — `params: t.Object({ id: t.String() })` imposes no `minLength` or UUID format constraint. Pre-existing pattern.
- **[optional]** `apps/api/src/routes/tasks.ts` — `TaskPatchBodySchema` does not set `additionalProperties: false`. Pre-existing pattern.
- **[optional]** `apps/api/src/routes/tasks.test.ts` — `patch` helper accepts `body: unknown`; `JSON.stringify(undefined)` produces `undefined` body.
- **[optional]** `apps/api/src/routes/tasks.test.ts` — 400 validation tests do not assert the seed POST succeeded. Pre-existing test-ergonomics pattern.
- **[optional]** `apps/api/src/routes/tasks.test.ts` — No test covers a PATCH with a `content-type: application/json` header but a non-JSON body.

## Deferred from: code review of 2-2-frontend-toggle-checkbox-sub-component-optimistic-mutation-completed-visual-treatment (2026-05-01)

- **[optional]** `apps/web/src/components/TaskRow.test.tsx` — `renderRowWithClient` captures `originalPatchFetch` at module evaluation time. Low risk given current test ordering.
- **[pre-epic-3]** `e2e/manage.spec.ts` — `waitForListSettled` resolves as soon as any list item is visible, which can be satisfied by pre-existing rows from prior test runs. Revisit when Story 3.1 (DELETE endpoint) enables a fixture-reset hook.
- **[optional]** `apps/web/src/data/toggleSyncStore.ts` — `clear(id)` sets the store key to `undefined` but does not delete it. Store grows monotonically over long sessions. Pre-existing pattern, mirrors `captureSyncStore.ts`.
- **[optional]** `apps/web/src/data/queries.test.tsx` — `capturedCancelCount` timing-sensitive under async reordering. Low risk today.
- **[pre-epic-3]** `apps/web/src/data/queries.ts` — `pendingToggleTimers` entries are never removed when a `TaskRow` component unmounts. If a task is deleted (Story 3.2) while a toggle timer is in-flight, the timer fires and writes to `toggleSyncStore` for a dead task ID. *Story 3.2 must add a cleanup signal or guard the timer callback against missing task IDs.*
- **[optional]** `apps/web/src/components/TaskRow.tsx` — When both `toggleSync` and `captureSync` have entries, capture retry is permanently masked. Architectural decision per AC5/spec.

## Deferred from: code review of 3-2-frontend-delete-deletebutton-optimistic-removal-animated-row-out-focus-landing-rule (2026-05-01)

- **[optional]** `apps/web/src/data/queries.ts:216` — Concurrent delete + toggle mutation: `cancelQueries` in `useDeleteTask.onMutate` cancels queries but not in-flight toggle mutations. If a toggle mutation is mid-flight when delete fires, toggle's `onError` may call `markExhausted` on a task that has already been optimistically removed from the cache and DOM. Pre-existing architectural constraint; the `onCleanup` timer cleanup mitigates the pending-timer path but not exhausted-state writes.
- **[optional]** `apps/web/src/components/TaskRow.test.tsx` — No unit test for the `isLeaving() === false` guard path on `animationend` (negative: spurious `animationend` fires when row is not leaving should not call `deleteMutation.mutate`). Low risk given current implementation; add alongside any future `animationName`-guard work.
- **[optional]** `e2e/manage.spec.ts:157` — E2E focus-landing tests assert `document.activeElement` after `.click()` but do not verify the "focus moves BEFORE `setIsLeaving(true)`" ordering constraint from AC #7. Tests rely on synchronous execution order being stable; add an intermediate `expect(...).toHaveAttribute` assertion to enforce the invariant if the focus logic is ever refactored.

## Deferred from: code review of 3-3-keyboard-delete-delete-and-backspace-on-focused-row-liveregion-announcements (2026-05-01)

- **[optional]** `apps/web/src/data/queries.ts:35` — `navigator.platform` is deprecated; spec explicitly accepted this tradeoff for MVP. Functional on real browsers (Chrome 120, Edge 120, Firefox 120, Safari 15). Revisit if a story adds user-agent detection for other features, or if `navigator.userAgentData.platform` achieves broader adoption.
- **[optional]** `apps/web/src/components/TaskRow.tsx:38` — Double-delete race: no `isLeaving()` guard before calling `handleDelete()` in the new keyboard handler. Pre-existing behavior (`handleDelete` unchanged from Story 3.2); `animationend` fires once per animation so actual double-mutation risk is low. Add `if (isLeaving()) return;` guard if future testing reveals an actual issue.
- **[optional]** `apps/web/src/data/queries.ts:37` — `firstDeleteAnnouncementSent` is not reset between E2E test sessions (page reload would reset it, but the flag lives in JS module state). No E2E test verifies announcement text, so this has no current test impact. By design: the flag is session-scoped per AC4/AC5.

## Deferred from: code review of 3-4-undosnackbar-with-cmd-ctrl-z-concurrent-delete-collapsing-restore-at-original-position (2026-05-01)

- **[optional]** `apps/web/src/data/queries.ts` — Timer expiry + slow DELETE: if the server-side delete takes >5 s, `onSuccess` fires after `undoCollapseTimer` has already called `clearAll()` and the snackbar has dismissed. The new `setEntry` call re-opens the store and the snackbar unexpectedly reappears for an old deletion. Requires tracking whether the window has expired before writing to the store; a clean fix would need a "window-open" flag or a deletion timestamp comparison.
- **[optional]** `apps/web/src/data/queries.ts:46` — `navigator.platform` is deprecated (pre-existing pattern from Story 3.3, already deferred in earlier code review of Story 3.3). No new exposure from this story.
- **[optional]** `e2e/manage.spec.ts` — `page.waitForTimeout(6000)` in the "5s window expires" E2E test adds at least 6 s to every CI run. Could be addressed by making `UNDO_WINDOW_MS` overridable via env var in test mode, or using Playwright's clock API to fake timers.

## Deferred from: code review of 3-1-backend-delete-api-tasks-id-idempotent (2026-05-01)

- **[optional]** `e2e/capture.spec.ts` — E2E `beforeEach` fixture reset (`DELETE /api/tasks`) is awaited without asserting the response status. If the endpoint returns non-204 (e.g. wrong environment, server error), tests continue with dirty state and produce confusing, non-deterministic failures. Fix: assert `res.status() === 204` in the beforeEach.
- **[optional]** `e2e/capture.spec.ts` — The `beforeEach` fixture reset depends on `IS_DEV=true`. No guard, skip condition, or environment check. Running E2E against any environment where `NODE_ENV=production` (staging, smoke tests) silently leaves stale data across tests. Assumed dev-only test environment per project architecture.
- **[optional]** `apps/api/src/routes/tasks.test.ts` — No test covers `DELETE /api/tasks` returning 404 when `IS_DEV=false`. The `!env.IS_DEV` branch in the fixture-reset route handler is the only new branch not exercised. Spec marks this optional; `env.IS_DEV` is structurally always `true` in Bun test runner (`NODE_ENV !== "production"`).
- **[optional]** `apps/api/src/routes/tasks.ts` — `params: t.Object({ id: t.String() })` on `DELETE /api/tasks/:id` imposes no `minLength` or UUID format constraint. Pre-existing pattern from Story 2.1; idempotent behavior handles unknown IDs correctly (204 regardless).
- **[optional]** `apps/api/src/env.ts` — `IS_DEV: NODE_ENV !== "production"` means any non-production value (including `staging`, `test`, `qa`) exposes the destructive `DELETE /api/tasks` endpoint. Broader architectural concern; out of scope for this story.

## Deferred from: code review of 4-2-tab-order-within-and-between-rows (2026-05-01)

- **[optional]** `e2e/keyboard.spec.ts` — SyncIndicator non-focusability only implicitly covered by the normal-row Tab sequence (Checkbox→DeleteButton skipping the `<span>`). No explicit negative assertion. `<span>` is architecturally non-focusable; implicit coverage is adequate for now.
- **[optional]** `e2e/keyboard.spec.ts` — Checkbox disabled-state tab order during in-flight toggle is not tested. No AC covers tabbing while a toggle mutation is pending and the Checkbox is `disabled`. Out of story scope.
- **[optional]** `e2e/keyboard.spec.ts` — RetryAction absence from normal rows only implicitly verified. The Checkbox→DeleteButton Tab sequence proves RetryAction is absent, but no explicit `toHaveCount(0)` assertion.
- **[optional]** `e2e/keyboard.spec.ts` — Focus restoration after ErrorMessage unmounts (successful retry) not tested. Out of scope; no AC covers post-retry focus management in this story.
- **[optional]** `e2e/keyboard.spec.ts` — Tab focus during the `.task-row--leaving` delete animation not tested. Out of scope; owned by Story 3.2/3.3 scope.

## Deferred from: code review of 4-3-escape-and-i-shortcut-to-return-focus-to-taskinput (2026-05-01)

- **[optional]** Duplicate `document.querySelector('[aria-label="New task"]')` selector hardcoded in 3 locations: `TaskRow.tsx` (×2) and `UndoSnackbar.tsx` (×1). If the `aria-label` ever changes, all three call-sites must be updated. Extract as a named constant or utility. Pre-existing pattern already established by the `ArrowUp` handler.
- **[optional]** `Escape` and `i` handlers in `handleRowKeyDown` have no guard for the case where a modal or `<dialog>` is open elsewhere on the page. `Escape` is the conventional dismiss key for dialogs; if a future overlay is added, the row handler fires concurrently and jumps focus to TaskInput unexpectedly. No modals exist today.
- **[optional]** `i` shortcut fires for any future text-input descendant of `<li>`. If an inline-edit field is ever added inside a task row, `i` keydown will steal focus to TaskInput rather than typing the character. Guard would need to check `event.target` element type.
- **[optional]** Double Escape collision: pressing Escape on a row returns focus to TaskInput (preserving draft); pressing Escape *again* on TaskInput clears the draft. The two-keypress sequence is intentional but untested as a combined flow.
- **[optional]** `RetryAction` `Escape`/`i` behavior relies on implicit event bubbling to the `<li>` handler — no explicit `onKeyDown` on the RetryAction button itself, unlike UndoSnackbar. If RetryAction ever gains `stopPropagation`, the shortcuts silently break. E2E covers regression.
- **[optional]** E2E RetryAction test: `page.unroute("**/api/tasks")` is called after asserting the Retry button is visible but without awaiting network idle or animation settle. Minor timing assumption; low-risk today.
- **[optional]** No unit test asserting `event.preventDefault()` was called for `i` and `Escape` shortcuts in `handleRowKeyDown`. The Space handler has an explicit `preventDefault` spy test. If the `preventDefault` call is removed in a future refactor, the only failing signal would be the E2E test.

## Deferred from: code review of 4-4-typing-anywhere-captures (2026-05-01)

- **[optional]** `e2e/keyboard.spec.ts` — E2e tab order assumption fragility: tests press Tab from TaskInput assuming it lands on the first row. Pre-existing pattern shared by all keyboard E2E stories; no new risk introduced.
- **[optional]** `e2e/keyboard.spec.ts` — "existing value" test fragility: `fill("draft")` + Tab to row + press `"a"` assumes Tab does not clear TaskInput value. Tests pass today; latent risk if TaskInput ever gains a Tab key handler.
- **[optional]** `apps/web/src/components/TaskRow.test.tsx` — `injectFakeInput` called per-test rather than in `beforeEach`. Style inconsistency with surrounding describe blocks; matches spec-specified pattern.
- **[optional]** `apps/web/src/components/UndoSnackbar.test.tsx` — Two new typing-anywhere tests create `fakeInput` inline inside the `it` body. If the outer `describe` block's `beforeEach` also injects a fake input, two elements with the same `aria-label` exist simultaneously; `querySelector` returns the first, potentially breaking the test's intent. Tests pass in practice; verify outer setup does not inject a duplicate.
- **[optional]** `apps/web/src/components/TaskRow.tsx`, `UndoSnackbar.tsx` — `if (taskInput)` null guard for absent `[aria-label="New task"]` element is untested. Silent no-op if the element is ever removed or renamed; add test when a conditional-render story is added.
- **[optional]** `event.key.length === 1` — known limitation: multi-codepoint emoji have `event.key.length > 1` and are excluded. Documented design tradeoff in Dev Notes; acceptable for current scope.
- **[optional]** `e2e/keyboard.spec.ts` — No E2e test exercises Ctrl+a / Meta+a from the UndoSnackbar Undo button. Modifier exclusion is E2e-tested for the row path only; Undo button path tested only at unit level.
- **[optional]** `apps/web/src/components/TaskRow.test.tsx` — No unit test inside `describe("TaskRow typing-anywhere-captures")` explicitly asserts that `i` pressed on `<li>` does NOT append. The `i` exclusion is validated indirectly by the adjacent `Escape and i shortcut` describe block.

## Deferred from: code review of 4-1-arrow-up-down-j-k-row-navigation (2026-05-01)

## Deferred from: code review of 4-5-focus-ring-audit-and-keyboard-only-playwright-spec (2026-05-05)

- **[optional]** `e2e/keyboard.spec.ts` — Outline accent color (`--color-accent-default`) not validated in focus-ring-audit assertions. AC #3 only requires `outline-width`, `outline-style`, and `outline-offset`; color verification is out of spec scope. Add a color assertion if a future story adds contrast-mode or theme-switching tests.
- **[optional]** `e2e/keyboard.spec.ts` — `task-row--leaving` class persistence on beta's DOM slot after undo is not explicitly asserted. The Journey 3 test relies on SolidJS `<Index>` reusing the leaving slot so `j`/`k` skips it, but no assertion confirms the class is present post-undo. Pre-existing SolidJS behavior; add assertion if navigation-after-undo becomes flaky.

- **[pre-epic-4]** `apps/web/src/components/TaskRow.tsx:36-51` — Story 4.4 typing-anywhere forward-compat hazard: if Story 4.4 adds a document-level keydown listener, its precedence vs. the row-level `j`/`k` handlers is unspecified. The epic spec excludes `j`/`k` from typing-anywhere-captures, but no integration contract or ordering guard exists today. Story 4.4 must define the precedence explicitly.
- **[optional]** `apps/web/src/styles/reset.css:32-38` — `:focus-visible` programmatic focus reliability: `.focus()` calls do not reliably trigger `:focus-visible` in all browser/session states (requires prior keyboard activity per Chrome heuristics). The app's focus ring depends on `:focus-visible` only; no `.task-row:focus` fallback. Acceptable given Playwright tests confirm the 2px outline in keyboard-activity context; revisit if a non-keyboard-initiated focus path reports invisible focus rings.
- **[optional]** `apps/web/src/components/TaskRow.tsx:38,44` — Global `document.querySelectorAll("[data-task-id]")` not scoped to the list container. Navigation reuses the same unscoped pattern already established by `handleDelete()`. If a future page ever renders multiple task lists or hidden rows (virtualization, portals), the index calculation breaks. Pre-existing architectural pattern; track alongside any multi-list story.
